import { redirect } from 'next/navigation';

export default function CompanyCreditsPurchase() {
    // For now, redirect to payment page since company credits purchase is not implemented
    redirect('/payment');
}
